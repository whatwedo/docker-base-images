# Editor backups, dumps and other server-side leftovers are never public content.
# Hits stay in the access log — they are probes, not noise.
location ~* (?:~|\.(?:bak|old|orig|save|swp|swo|sql|sqlite3?))$ {
    return 404;
}
