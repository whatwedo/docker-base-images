# Hidden files (beginning with a period) are never served. /.well-known/ is
# public by definition (ACME, OIDC discovery, security.txt).
location ~ /\.(?!well-known/) { access_log off; log_not_found off; deny all; }
