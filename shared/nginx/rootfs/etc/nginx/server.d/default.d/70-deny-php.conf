# Loaded after the PHP entrypoint locations of each image, so those win nginx's
# first-matching-regex selection. Every other executable PHP path is rejected
# instead of being returned as static source code.
location ~* \.(?:php[0-9]*|phps|phar|pht(?:ml?)?|inc)(?:[.~/]|$) {
    return 404;
}
